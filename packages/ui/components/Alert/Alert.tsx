import * as React from 'react'
import { AlertProps } from './Alert.types'

// try husky 2
export const Alert = (props: AlertProps) => {
  return <div className='p-4 text-white bg-red-500'>this is an alert</div>
}
